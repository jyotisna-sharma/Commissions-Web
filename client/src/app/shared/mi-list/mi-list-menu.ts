import { MiListMenuItem } from './mi-list-menu-item';

export class MiListMenu {
     visibleOnDesk: boolean; // show mat menu when view on desktop (large screen).
     visibleOnMob: boolean;
    menuItems: MiListMenuItem[];
}
